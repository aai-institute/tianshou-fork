defaults:
  - experiment/ppo_with_actor_critic@_here_
  - _self_

#algo: "ppo"
#task: "Ant-v4"
#seed: 0

#experiment:
#  env_factory:
#    task: ${task}
sampling_config:
  num_epochs: 10
#    train_seed: 42
#    test_seed: 1337

hydra:
  mode: MULTIRUN
#  run:
#    dir: ../${now:%y-%m-%d}/${now:%H-%M-%S}/${experiment_name}
  sweeper:
    params:
#      task: Pendulum-v1,HalfCheetah-v4
      sampling_config.train_seed: 42,43
      sampling_config.test_seed: 1337,1338
      env_factory.task: Pendulum-v1,HalfCheetah-v4
#      repetition: range(2)
#      experiment:
#        sampling_config:
#          train_seed: range(2)
#          test_seed: range(2)
#        env_factory:
#          task: ["Ant-v4","Pendulum-v1"]
  sweep:
    dir: log/${now:%y-%m-%d}/${now:%H-%M-%S}/ppo/${env_factory.task}
    subdir: train_seed_${format:${sampling_config.train_seed}, '{:02d}'}/test_seed_${format:${sampling_config.test_seed}, '{:02d}'}